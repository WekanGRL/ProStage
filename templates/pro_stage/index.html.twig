{% extends 'base.html.twig' %}

{% block contenu %}
    
    <h1 style="color:green"> <b><u><center> Bienvenue sur la page d'accueil de ProStage </b></u></h1>

    <table class="table table-hover">
        <thead>
            <tr>
                <div class="row">
                    <div class="col-md-6 col-xs-12">
                        <td style="font-size:25px"><b><u>Titre du stage</u></b></td>
                    </div>

                    <div class="col-md-6 col-xs-12">
                        <td style="font-size:25px"><b><u>Descriptif du stage</u></b></td>
                    </div>
                </div>
            </tr>
        </thead>

        {% for stageCourant in stagesAvecEntreprise %}
            <tbody>
                <tr>
                    <div class="row">
                        <div class="col-md-6 col-xs-12">
                            <td>
                                <a href="{{path('pro_stage_stages', {id:stageCourant.id})}}">
                                    {{ stageCourant.titre }} proposé par {{stageCourant.entreprises.nom}}
                                </a>
                            </td> 
                        </div>
                        <div class="col-md-6 col-xs-12">
                            <td>
                                <a href="{{path('pro_stage_stages', {id:stageCourant.id})}}">
                                    {{ stageCourant.description }}
                                </a>
                            </td>
                        </div>
                    </div>
                </tr>
            </tbody>
            {% endfor %}
    </table>
{% endblock %}
