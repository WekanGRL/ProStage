{% extends 'base.html.twig' %}
{% block contenu %}

    <div>
        <h1>Stages proposés pour {{nomFormation}} :</h1>
        <div class="card">
            <div>
                {% for stageCourant in stagesParNomFormation %}
                    <td><a href="{{path('pro_stage_stages', {id:stageCourant.id})}}">{{ stageCourant.titre }}</a></td>
                    <br/>
                {% endfor %}
            </div>

        </div>
    </div>

{% endblock %}