{% extends 'base.html.twig' %}

{% block contenu %}
    <h1> Détails de : {{ formation.nomLong }} </h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Nom long</th>
                <td>{{ formation.nomLong }}</td>
            </tr>
            <tr>
                <th>Nom court</th>
                <td>{{ formation.nomCourt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('formation_index') }}">Retour à la liste</a>

    <a href="{{ path('formation_edit', {'id': formation.id}) }}">Modifier</a>

    {{ include('formation/_delete_form.html.twig') }}
{% endblock %}
